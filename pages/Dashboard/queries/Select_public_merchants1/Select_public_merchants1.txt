SELECT
  *
FROM
  public.merchants
ORDER BY
  "{{Dashboard_Table.sortOrder.column || 'id'}}" {{Dashboard_Table.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{Dashboard_Table.pageSize}}
OFFSET
  {{Dashboard_Table.pageOffset}}